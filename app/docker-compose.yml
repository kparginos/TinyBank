version: "3.9"
services:
    web:
        build: .
        dockerfile: Dockerfile
        ports:
            - "8000:80"
        depends_on:
            - db
    db:
        image: "mcr.microsoft.com/mssql/server"
        environment:
            SA_PASSWORD: "admin!@#123"
            ACCEPT_EULA: "Y"